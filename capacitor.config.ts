import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.digam-starter.app',
  appName: 'Digam Starter App',
  webDir: 'www'
};

export default config;


